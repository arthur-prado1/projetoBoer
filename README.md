# projetoBoer

Aplicativo em Expo/React Native com Firebase e backend Node.js para API de produtos.

## Como rodar o app

```bash
npm install
npm run web
```

## Como rodar o backend

```bash
cd backend
npm install
npm run dev
```
